services:
  evio:
    build: .
    security_opt:
      - seccomp=unconfined
    volumes:
      - ${WORKSPACE:-.}:/app
    working_dir: /app
    user: devcontainer
    tmpfs:
      - /tmp:mode=777
    environment:
      - WORKSPACE=${WORKSPACE:-.}
      - PRE_COMMIT_HOME=/app/.cache/pre-commit
